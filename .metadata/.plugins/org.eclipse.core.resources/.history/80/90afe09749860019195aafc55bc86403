package Streams;

import java.util.Arrays;

public class StreamsWithIntArray {

	public static void main(String[] args) {
		
		//average of the squares of an int array
		Arrays.stream(new int[] {2, 4, 6, 8, 10})
		.map(x -> x * x)
		.average()
		.ifPresent(System.out::println);
	}
}
